function mergesort(low, high, array, subarr) {
    if (array.length <= 1){
        return array;
    }
    while (array.length > 1){
       var mid = Math.floor((low+hi)/2);
        
    }
    let size =1;
    
    return array;
}
